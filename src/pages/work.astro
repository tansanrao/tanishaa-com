---
import Layout from '../layouts/Layout.astro';
import WorkIndex from '../components/work/WorkIndex.astro';
import { getWorkProjects } from '../lib/work';

const projects = await getWorkProjects('studio');
---

<Layout pageTitle="Tanishaa Rao — Work" activeNav="work">
  <WorkIndex projects={projects} activeCategory="studio" />
</Layout>
